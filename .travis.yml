language: cpp
compiler: g++

addons:
  apt:
    sources:
      - george-edison55-precise-backports
      - ubuntu-toolchain-r-test
    packages:
      - cmake
      - gcc-5
      - libsqlite3-dev

install:
- export CXX="g++-5"
